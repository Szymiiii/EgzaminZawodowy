1.
SELECT `nazwa`, `dlugosc`, `szerokosc` FROM `smok` WHERE pochodzenie = "Polska";
2.
SELECT DISTINCT(pochodzenie) FROM `smok` ORDER BY `smok`.`pochodzenie` ASC
3.
SELECT parada.rok, AVG(smok.dlugosc) AS "Średnia długość" FROM `parada` INNER JOIN udzial ON udzial.id_parada = parada.id INNER JOIN smok ON smok.id = udzial.id_smok WHERE parada.rok>2005 GROUP BY parada.rok;
4.
ALTER TABLE `parada` ADD `lokalizacja` VARCHAR(100) NOT NULL AFTER `atrakcje`;