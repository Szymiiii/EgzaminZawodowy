1.
SELECT `imie`, `nazwisko` FROM `pracownicy` WHERE staz <5;
2.
SELECT AVG(pracownicy.pensja), stanowiska.nazwa FROM `pracownicy` INNER JOIN stanowiska ON stanowiska.id = pracownicy.stanowiska_id GROUP BY stanowiska.nazwa;
3.
SELECT `imie`, `nazwisko`, MAX(`pensja`) FROM `pracownicy` LIMIT 1;
4.
UPDATE pracownicy SET staz = staz + 1; 